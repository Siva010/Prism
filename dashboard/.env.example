# The gateway's admin API. The dashboard is a read-only client of it and holds
# no database credentials of its own -- tenant scoping is enforced by the key,
# server-side, so a dashboard session can never see another tenant's traffic.
PRISM_URL=http://localhost:8000
PRISM_API_KEY=prism_sk_...
